-- SCEPTIC-NVIM: entrypoint del starter (basado en LazyVim).
-- Carga el bootstrap de lazy.nvim y la configuracion; el resto de modulos
-- de lua/config/ (options, keymaps, autocmds) los carga LazyVim por su cuenta.
require("config.lazy")
